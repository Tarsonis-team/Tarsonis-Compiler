#include "AST-node.hpp"
#include "visitor/abstract-visitor.hpp"

namespace parsing {

// void ASTNode::accept(IVisitor& visitor) {
//     visitor.visit(*this);
// }

// void ASTNode::accept(IVisitor&& visitor) {
//     visitor.visit(*this);
// }

}  // namespace parsing
