enum class VarType {
    INT,
    FLOAT
};

enum class MathType {
    PLUS,
    MINUS,
    MUL,
    DIV,
    EQUAL,
    GREATER,
    LESS
};

enum class BracesType {
    CURLY,
    SQUARE,
    ROUND
};

enum class PunctuationType {
    SEMICOLON,
    DOUBLE,
    COMMA,
    POINT,
    ARROW,
    ASSIGN,
    END,
    THEN,
    ELSE,
    LOOP
};
