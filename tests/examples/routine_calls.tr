type Array is array[5] integer

routine two(integer x, Array lst) -> integer is
    var a: integer;
    return 2 + a;
end

routine main() is
    var c: integer is two();
    two(1, 2);
    two(two(3), 4);
end

