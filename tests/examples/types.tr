type socketFD is integer;

routine main() is
    var socket: socketFD;

    socket := 8;

    var r: real is 1.4;
    r := r + 1.5;
    print(r);

    var b: boolean is true;
    b := b and false;
    print(b);

    print(socket);
end
