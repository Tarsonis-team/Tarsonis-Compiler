routine main() -> integer is
    var a: integer is 4 + 2 / 1 - (-8 + 3);
    var b: integer is 8;
    var d: integer;

    if true then
        return d;
        d := 2;
    end

    var c: integer is a / a + a * b - a / b
    c := a - b;
    return c;
end
