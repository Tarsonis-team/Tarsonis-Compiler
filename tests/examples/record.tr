type Point is record
    var x: integer
    var y: integer
end


routine main() is
    var point: Point;

    point.x := 5;
    point.y := 3;

    var x: integer is point;

    print(point.x + point.y);
end
