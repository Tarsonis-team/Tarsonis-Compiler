type SomeArr is array[5] integer;

routine two(integer t) -> integer is
    return 2 + t;
end


routine main() is
    var some_arr: SomeArr;
    var arr: array[2] SomeArr;

    var new_arr: array[3] integer;

    var a: integer is 2 + 1;
    var d: integer is two(5);

    some_arr := arr[1];
    d := 1 + some_arr[a] + arr[0][0];
    if d then
        var c : integer is 5
    end

    while d loop
        var k : integer is 10;
    end

    for i in 1 .. 3 loop
    end

    print(1);
    print(a);
end
