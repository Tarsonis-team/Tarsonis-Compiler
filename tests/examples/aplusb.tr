routine two(integer t) -> integer is
    return 2 + t;
end


routine main() is
    var a: integer is 2 + 1;
    var d: integer is two();

    d := 1 + a;
    if d then
        var c : integer is 5
    end

    while d loop
        var k : integer is 10;
    end

    for i in 1 .. 3 loop
    end
end
